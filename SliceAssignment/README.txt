/** Installation Guild **/
Local Deploy:
HTTP endPoint: http://localhost/SliceAssignment/ ---get the input page
			   http://localhost/SliceAssignment/submit ---submit url, render after click "submit" button on input page
Context-root: /Slice/Assignment/

build:
the package includes build.gradle script, it's available to build if Gradle already installed at your Environment
Firstly, navigate to SliceAssignment folder
then $SliceAssignment: gradle build 